using System;
using System.Runtime.InteropServices;

namespace Facade
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            //Facade is used to simplify the process which calls several complicated APIs with simple one.
            //Facade put a simple interface in front of the complicated subsystems.
        }
    }
}